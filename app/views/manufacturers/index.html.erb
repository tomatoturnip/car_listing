<% @manufacturers.each do |manufacturer| %>
 <div>
    <p><strong><%= manufacturer.name %></strong>
    <p><%= manufacturer.country %></p>

    <%= link_to manufacturer.name, manufacturer %>
    <%= link_to "Destroy #{manufacturer.name}", manufacturer, method: :delete %>
  </div>
<% end %>
